# NPM configuration for timeline-of-us-ai
#
# AUDIT NOTES:
# - live-server (dev-only) has transitive vulnerabilities in chokidar, braces, micromatch
# - These vulnerabilities ONLY affect local development (not production)
# - The application itself has zero production dependencies
# - To verify production is secure: npm audit --omit=dev
#
# For CI/CD, use: npm audit --omit=dev --audit-level=moderate
audit-level=moderate
